interface:
  display_name: "Frontend Auth Flow Contracts"
  short_description: "Auth flow browser contracts."
  default_prompt: "Use $frontend-auth-flow-contracts to review frontend auth flows: login/signup/password reset, email verification, OTP, passkeys/WebAuthn, auth callbacks, returnTo, autofill, safe errors, and fresh verification."
