# Skill evidence coverage

This map shows where each skill gets its supporting evidence. It is not a bug list. Use it to see which rung of the evidence ladder below a skill's support sits on.

## Evidence ladder

One ladder is used everywhere in this repository. Each rung says **what was
actually done**, not how confident anyone feels about it. Confidence ratings and
verification steps used to be separate axes in separate documents, which let a
row read as stronger than the work behind it.

| Rung | Name | What it means | Where it lives |
| --- | --- | --- | --- |
| E1 | Measured | A run happened and its result is recorded, including when the result was negative. | `evals/behavioral/`, `evals/routing/results/`, one row in `docs/oss-validation-cases.md` (pack-effect runs); CI browser fixtures under `skills/*/tests/` (behavior-reality gates, not pack-effect evidence) |
| E2 | Source-verified | The cited lines exist at a pinned commit and say what the row claims. Not reproduced, not filed, not accepted by anyone upstream. | `docs/oss-validation-cases.md` |
| E3 | Primary-source cited | The claim traces to a specification, vendor documentation, or a browser bug tracker. | each `SKILL.md` and its `references/*.md` |
| E4 | Routing example | A synthetic case that helps choose between skills. Makes no claim about any external codebase. | `evals/routing/cases.json`, `skills/*/evals/evals.json` |

Two things this ladder deliberately makes visible.

E1 is nearly empty where it matters most. Only four runs measure whether this
pack changes what an agent does: two behavioral comparisons that both tied, one
routing comparison covering 16 of 138 cases, and one Figma-vs-Storybook pixel
diff on an n8n Callout story. That is the honest state of the pack's own
central claim.

A second kind of E1 is not nearly empty, but it is a different kind.
`.github/workflows/checks.yml` re-runs the browser fixtures under
`skills/*/tests/` on every push. Four of the 41 skills have them: the
history-scroll-restoration, media-capture-device, and pointer-gesture suites
run across Chromium, Firefox, and WebKit, and the iframe-embed suite runs on
Chromium only. These runs verify that the browser behaviors those skills
describe are real; they do not measure whether handing an agent a skill changes
its output. They also fall short of this rung's own definition in one respect:
the same suite gates pushes through `scripts/pre-push-checks.sh`, so a failing
fixture blocks the push instead of leaving a recorded negative result. The only
record is CI run history. The two kinds are counted separately for that reason.

E2 is not a defect claim. It means someone opened the file and the code is
there. Whether the pattern is a bug the maintainer would accept is a separate
question that no row in this repository has answered, because nothing has been
reproduced locally or filed upstream.

## Coverage table

| Skill | Evidence home | Current status | Add more only when |
| --- | --- | --- | --- |
| `webview-bridge-pages` | `skills/webview-bridge-pages/references/*.md`, `docs/oss-validation-cases.md` | Broad coverage: primary-source WebView dossier, host-specific references, validation cases, and source-verified leads. Bridge timing, READY/auth buffering, renderer death, viewport/safe-area, paint-vs-hit-test, and host lifecycle are represented. | A new WebView host or engine behavior is supported by official docs, browser bugs, or a reproduced app-WebView case. |
| `iframe-embed-contracts` | `skills/iframe-embed-contracts/SKILL.md`, `skills/iframe-embed-contracts/references/prior-art.md`, `skills/iframe-embed-contracts/tests/*` | Primary-source coverage for embeddability policy, sandbox/Permissions Policy, postMessage origin/source/schema checks, handshake timing, partitioned storage, and iframe accessibility; a bounded public-skill/issue search records strong prior art without claiming uniqueness; a live two-origin Playwright/Chromium fixture covers handshake, rejection, grow/shrink sizing, teardown, title, and `frame-ancestors`. | A case includes both parent and guest evidence and is not merely a generic CSP, payment-field, or native-WebView concern. |
| `browser-page-lifecycle-bfcache-contracts` | `skills/browser-page-lifecycle-bfcache-contracts/SKILL.md`, `skills/browser-page-lifecycle-bfcache-contracts/evals/evals.json` | Primary-source references plus evaluation cases for persisted Back/Forward restoration, stale-state reconciliation, resource pause/resume, eligibility diagnostics, and weak-finding rejection. | A case includes a real top-level browser history traversal and distinguishes persisted restore from first-load routing, service-worker bytes, transport resume, or native WebView lifecycle. |
| `history-scroll-restoration-contracts` | `skills/history-scroll-restoration-contracts/SKILL.md`, `skills/history-scroll-restoration-contracts/evals/evals.json`, `skills/history-scroll-restoration-contracts/tests/*` | Primary-source and public-issue references, evaluation cases, and a live bundled Chromium/Firefox/WebKit fixture for same-document entry identity, asynchronous layout readiness, exact restoration, and no second jump. The fixture distinguishes its same-`Document` scope from bfcache and branded-Safari behavior. | A case reproduces SPA Back/Forward or same-document hash traversal and distinguishes scroll/fragment restoration from cross-document bfcache resource reconciliation, first-load deep-link initialization, and virtualized-list measurement. |
| `media-capture-device-contracts` | `skills/media-capture-device-contracts/SKILL.md`, `skills/media-capture-device-contracts/evals/evals.json`, `skills/media-capture-device-contracts/tests/*`, `skills/media-capture-device-contracts/references/real-device-validation.md` | Primary-source and public-issue references, evaluation cases, a Chromium fake-device fixture for real `getUserMedia()` acquisition, and a bundled Chromium/Firefox/WebKit synthetic-`MediaStream` matrix for application-owned attachment, replacement, late resolution, and teardown. A separate manual evidence contract covers native permission UI and real hardware without claiming it ran. | A case includes real `MediaStream`/`MediaStreamTrack` state evidence and distinguishes capture lifecycle from iframe permission delegation, a distinct gesture-gated API/timing failure, and generic privacy/security review. |
| `a11y-contract-testing` | `skills/a11y-contract-testing/SKILL.md`, `docs/oss-validation-cases.md` | E2 source-verified coverage for role/name/state regression testing. | A case shows a role/name/state regression that focused tests would catch better than a generic checklist. |
| `semantic-markup-contracts` | `skills/semantic-markup-contracts/SKILL.md`, `docs/oss-validation-cases.md` | Covered through markup/a11y overlap cases. The current evidence exercises native HTML and ARIA role/state contracts, but most maintainer-facing fixes are grouped under accessibility regression coverage. | A native-HTML-first case is not primarily an accessibility test case: labels, headings, button/link semantics, table/list semantics, or invalid nesting. |
| `overlay-focus-scroll-contracts` | `skills/overlay-focus-scroll-contracts/references/overlay-contracts.md`, `docs/oss-validation-cases.md` | Has reference guidance plus E2 casebook coverage for focus, inert/aria-hidden, nested overlays, and scroll lock cleanup. | A case includes runtime open/close/unmount evidence rather than only static component code. |
| `pointer-gesture-contracts` | `skills/pointer-gesture-contracts/SKILL.md`, `skills/pointer-gesture-contracts/evals/evals.json`, `skills/pointer-gesture-contracts/tests/*`, `skills/pointer-gesture-contracts/references/real-device-validation.md` | Primary-source and public-issue references, evaluation cases, and a bundled Chromium/Firefox/WebKit fixture for trusted mouse delivery, pointer capture, cleanup, and emulated touch tap delivery. Synthetic cancel/recovery paths and a manual physical-device evidence contract are explicit; physical touch and pen remain unexecuted. | A case captures the full single-pointer sequence and distinguishes direct-manipulation ownership from file drag/drop ingestion, semantic click/keyboard behavior, overlay focus, library-only handler style, and out-of-scope pinch/rotate/multi-contact geometry. |
| `contenteditable-selection-contracts` | `skills/contenteditable-selection-contracts/SKILL.md`, `skills/contenteditable-selection-contracts/evals/evals.json` | Primary Selection/Input/UI/HTML/Clipboard sources, public recurrence evidence, and eight evaluation cases for live editing-host selection, single-owned `beforeinput`/`input` transactions, composition, undo/redo, paste/drop, focus, teardown, false positives, and sibling routes. | A real edit sequence shows selection or transaction ownership failing across DOM replacement, composition, history, paste/drop, focus, or teardown rather than only finding `contenteditable`, `Range`, or `innerHTML`. |
| `cjk-text-and-input` | `skills/cjk-text-and-input/SKILL.md`, `docs/oss-validation-cases.md` | E2 source-verified coverage for IME composition and CJK input timing. | A case includes CJK/IME reproduction, not just a generic Enter key handler. |
| `i18n-copy-and-layout` | `skills/i18n-copy-and-layout/references/*.md`, `docs/oss-validation-cases.md` | Has E2 casebook coverage for localized copy, pluralization, expansion, and locale formatting. | Evidence shows locale-specific failure or copy/layout contract, not just untranslated text. |
| `datetime-correctness` | `skills/datetime-correctness/references/*.md`, `docs/oss-validation-cases.md` | E2 source-verified coverage for timezone, date-only, and conversion hazards. | A case pins zone, DST, date-only, or instant-vs-wall-clock behavior with a failing expectation. |
| `constraint-validation-contracts` | `skills/constraint-validation-contracts/references/validation-api-and-states.md`, `docs/oss-validation-cases.md` | E2 source-verified coverage for `setCustomValidity`, native validity state, and invalid-then-valid lifecycle. | The issue depends on native Constraint Validation API semantics rather than a JS form library. |
| `js-form-validation-contracts` | `skills/js-form-validation-contracts/references/library-validity-lifecycle.md`, `docs/oss-validation-cases.md` | Has source-backed form-library lifecycle cases and source-verified leads. | The case involves library-owned error/validity/submit/server-field state. |
| `ssr-hydration-mismatch` | `skills/ssr-hydration-mismatch/references/triggers-and-containment.md`, `docs/oss-validation-cases.md` | Has E2 casebook coverage for first-render determinism and containment. | Evidence separates URL/auth/data state from server-client DOM mismatch. |
| `deeplink-hydration` | `skills/deeplink-hydration/SKILL.md`, `docs/oss-validation-cases.md` | E2 source-verified coverage for route/query/auth-bounce readiness. | The bug starts from direct navigation, refresh, callback, or auth redirect state. |
| `realtime-transport-contracts` | `skills/realtime-transport-contracts/SKILL.md`, `skills/realtime-transport-contracts/references/transport-resilience.md` | Primary-source reference coverage for reconnect backoff, SSE resume, delta ordering, heartbeat/zombie detection, backpressure, and socket auth refresh. | The case has connection-drop or long-lived-stream evidence, not only a generic subscription cleanup issue. |
| `frontend-auth-flow-contracts` | `skills/frontend-auth-flow-contracts/SKILL.md`, `skills/frontend-auth-flow-contracts/references/browser-auth-contracts.md`, `docs/oss-validation-cases.md` | Has official/browser-contract references plus E2 casebook coverage. | The case is auth-specific: returnTo, OAuth/passkey, autocomplete, OTP expiry/retry, safe errors, or fresh verification. |
| `user-activation-contracts` | `skills/user-activation-contracts/SKILL.md`, `skills/user-activation-contracts/evals/evals.json` | Primary-source references plus evaluation cases for transient/sticky activation, async or consumed gesture boundaries, blocked API recovery, and sibling-policy handoff. | A real gesture-gated API is traced from a trusted event to denial or consumption; permission, insecure-context, iframe-policy, and action-specific data issues remain with sibling skills. |
| `frontend-security-baseline` | `skills/frontend-security-baseline/references/*.md`, `docs/oss-validation-cases.md` | Has broad security references plus validation cases and leads. | Evidence shows a concrete browser source-to-sink or policy contract, not only a scary API name. |
| `bff-proxy-security-contracts` | `skills/bff-proxy-security-contracts/SKILL.md`, `skills/bff-proxy-security-contracts/references/prior-art.md`, `skills/bff-proxy-security-contracts/evals/evals.json` | Primary-source and open-source comparison for SSRF allowlists, configured proxy routes, method/auth/header policy, alternate ingress, multipart budgets/boundaries, and persistent business-flow controls; eval prompts cover proxy, upload, and replay decisions. | A frontend-owned server route relays upstream HTTP and the evidence identifies the client-controlled capability or a cross-ingress policy gap. |
| `payment-page-client-security` | `skills/payment-page-client-security/references/pci-payment-page-scope.md`, `docs/oss-validation-cases.md`, `README.md` | Has payment-page scope guidance and source-verified leads. It collects frontend evidence, not PCI scope decisions. | A case has runtime script inventory or PAN/CVV boundary evidence for a real checkout page. |
| `optimistic-update-rollback-contracts` | `skills/optimistic-update-rollback-contracts/SKILL.md` | Primary-source reference coverage for optimistic apply, rollback, temp/server id reconciliation, refetch races, and concurrent mutation ordering. | Evidence shows the apply -> confirm/rollback -> reconcile sequence, not only a generic mutation failure. |
| `file-ingest-contracts` | `skills/file-ingest-contracts/SKILL.md`, `skills/file-ingest-contracts/references/file-ingest-contracts.md` | Primary-source reference coverage for drag/drop, paste, directory/multiple upload, type trust, and object-URL preview lifecycle. | The case involves files entering the page; export/download issues belong to `download-export-safety`. |
| `download-export-safety` | `skills/download-export-safety/references/export-contracts.md`, `docs/oss-validation-cases.md` | Has E2 casebook coverage for CSV/Excel, Blob/Object URL, clipboard, filenames, and export boundaries. | Evidence inspects final exported cells/files or lifecycle cleanup, not only helper names. |
| `design-to-code-fidelity` | `skills/design-to-code-fidelity/references/*.md`, `skills/design-to-code-fidelity/scripts/*`, `docs/oss-validation-cases.md` | Strong tooling-backed coverage. It has capture/diff scripts, Figma adapters, mismatch interpretation, and E2 casebook coverage. | The case has a design reference and implementation capture, or explicitly states why it remains [T3/T4 evidence](../skills/design-to-code-fidelity/SKILL.md#evidence-tiers). |
| `component-extraction-judgment` | `skills/component-extraction-judgment/references/prior-art.md`, `docs/oss-validation-cases.md` | Source-verified leads plus React/Vue/Svelte/Web Components composition references. This is a refactor/maintenance judgment skill, not a runtime-defect detector. | A case has stable duplication evidence and a bounded extraction or non-extraction decision. |
| `client-error-observability-contracts` | `skills/client-error-observability-contracts/SKILL.md` | Primary-source reference coverage for `window.onerror`, `unhandledrejection`, framework error boundaries, cross-origin stack blackout, source maps, grouping, and PII scrubbing. | The case is about capturing and making frontend errors actionable; security policy belongs to `frontend-security-baseline`. |
| `frontend-report-triage` | `skills/frontend-report-triage/SKILL.md`, `skills/frontend-report-triage/references/triage-examples.md` | Routing only. It has synthetic examples that choose follow-up skills and evidence gaps. | A recurring messy-report pattern needs a better route, not a new domain claim. |
| `view-transitions-contracts` | `skills/view-transitions-contracts/SKILL.md` | Primary-source reference coverage for View Transitions API review risks: duplicate names, stale snapshots, reduced motion, React update wrapping, interruption, and top-layer/iframe blind spots. | The case uses `document.startViewTransition` or `::view-transition-*`, not ordinary CSS enter/exit transitions. |
| `css-transition-animation-contracts` | `skills/css-transition-animation-contracts/SKILL.md` | Primary-source reference coverage for discrete-property enter/exit transitions, `@starting-style`, `transition-behavior`, `overlay`, `::backdrop`, and completion-event cleanup. | The case depends on CSS transition/animation lifecycle; overlay focus/scroll ownership stays in `overlay-focus-scroll-contracts`. |
| `responsive-image-contracts` | `skills/responsive-image-contracts/SKILL.md` | Primary-source reference coverage for `srcset`, `sizes`, intrinsic-width labels, LCP image priority, art direction, and CLS prevention. | The issue is image-source selection or image layout reservation, not whole-page Core Web Vitals attribution. |
| `money-and-precision-contracts` | `skills/money-and-precision-contracts/SKILL.md`, `skills/money-and-precision-contracts/references/precision-contracts.md` | Primary-source reference coverage for float drift, integer minor units, decimal libraries, rounding modes, summing order, and localized amount parsing. | The case involves browser arithmetic/precision/storage, not only display formatting. |
| `core-web-vitals-performance-contracts` | `skills/core-web-vitals-performance-contracts/SKILL.md`, `docs/oss-validation-cases.md` | Source-verified leads plus primary-source reference coverage for LCP, CLS, INP, and TTFB attribution. | The case ties a field/lab metric to a specific element, layout shift, long task, or response bottleneck. |
| `frontend-data-fetching-cache-contracts` | `skills/frontend-data-fetching-cache-contracts/SKILL.md`, `docs/oss-validation-cases.md` | Source-verified leads plus primary-source reference coverage for query keys, invalidation, request waterfalls, refetch storms, infinite lists, and cache policy drift. | The data library or client cache owns the stale/wasteful read; raw effect races belong to `async-effect-race-contracts`. |
| `browser-storage-durability-contracts` | `skills/browser-storage-durability-contracts/SKILL.md`, `skills/browser-storage-durability-contracts/evals/evals.json` | Primary IndexedDB and StorageManager sources, public prior art, and 13 evaluation cases for blocked upgrades, transaction activity, stale-write prevention, commit/abort evidence, quota/persistence, hidden worker ownership, truthful recovery, false positives, and sibling routes. | Runtime evidence ties browser-local data loss or a false durability claim to open/upgrade ownership, transaction lifetime or completion, quota/persistence, unexpected closure, or recovery rather than service-worker bytes, client query caches, security policy, or a proposed sync system. |
| `async-effect-race-contracts` | `skills/async-effect-race-contracts/SKILL.md`, `docs/oss-validation-cases.md` | Source-verified leads plus primary-source reference coverage for fetch-on-deps races, missing cleanup/AbortController, StrictMode double invoke, and stale closures. | The bug sits below a data library in raw async effects, subscriptions, timers, or event handlers. |
| `pwa-offline-cache-contracts` | `skills/pwa-offline-cache-contracts/SKILL.md`, `docs/oss-validation-cases.md` | Source-verified leads plus primary-source reference coverage for service-worker update flow, stale build/chunk errors, precache gaps, cache cleanup, and authenticated-response caching. | The issue is Cache Storage or service-worker asset behavior, not an in-memory data-fetch cache. |
| `large-list-data-grid-contracts` | `skills/large-list-data-grid-contracts/SKILL.md`, `docs/oss-validation-cases.md` | Source-verified leads plus primary-source reference coverage for virtualization scroll, overscan, accessibility totals, focus retention, find-in-page, and pinned layout drift. | The case involves windowing/virtualization behavior that differs between production and render-all tests. |
| `resize-observer-layout-contracts` | `skills/resize-observer-layout-contracts/SKILL.md`, `skills/resize-observer-layout-contracts/evals/evals.json` | Primary-source references plus evaluation cases for pre-paint delivery, size feedback cycles, box selection, stale targets, cleanup, and weak-finding rejection. | A case demonstrates a callback-induced size cycle, wrong box, stale target, or cleanup leak outside iframe sizing and virtualization measurement contracts. |

## Update rule

When adding a skill, add at least one row here before promoting it in README. The row can say `Routing only` or `Leads only`; do not invent validation cases to make coverage look complete.

## Source link maintenance

Link checking runs at two scopes, because the two failure modes have different causes:

- **Pre-push (automatic):** `scripts/pre-push-checks.sh` checks only the markdown files you are pushing (`--link-paths`), so a citation you just added is verified before it lands — roughly two seconds. It skips itself when offline, and `SKIP_LINK_CHECK=1` forces a skip.
- **Weekly (`.github/workflows/link-check.yml`) and before a release:** `python3 scripts/audit-skill-pack.py --check-links` checks all external URLs. This catches rot in files nobody has touched, which no pre-push hook can see.

Only `404`/`410` fail a run — a dead citation is a real defect. Unreachable-but-not-dead URLs (bot-blocked `403`, rate-limited `429`, timeouts) are listed as unverified without failing, because transient failures otherwise make the gate flap between runs; spot-check those in a browser once.

For each dead link, decide in this order:

1. **Moved, same content** — the site restructured but the successor page still states the claim: swap the URL, keep the annotation.
2. **Content exists only historically** — the file or page was removed upstream: pin what the claim referenced (a commit-SHA permalink for repository files, a Web Archive snapshot for pages) and say why in the link annotation.
3. **Claim no longer holds** — the source moved *and* its current version says something else: this is not a link fix. Re-verify the claim against current primary sources and update or remove the claim and its citation together.
4. **No replacement found** — never ship a dead or invented citation: soften or remove the claim along with the link.

A replacement URL must both resolve (rerun `--check-links`) and actually state what the claim needs — read the target, do not trust the status code. Record old → new and the reason in the commit message.
